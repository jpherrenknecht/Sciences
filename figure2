from fractal import koch
import numpy as np
import matplotlib.pylab as plt

a = np.array([[0], [0]])
b = np.array([[1], [0]])
plt.figure(figsize=(10, 10 * np.sqrt(1 / 3.) / 2))
koch(a, b, 5, np.pi / 3.)
plt.axis('off')
plt.show()
